import React from 'react';
import NavBar from './NavBar';

const HomePage = () => {
    return (
        <div className = "home">
            <NavBar/>
        </div>
    );
};

export default HomePage;